#include "main.h"

/**
 * _strlen - returns the lenght of a string
 *
 * @s: pointer for a char
 *
 * Return: An integer
*/

int _strlen(char *s)
{
	int length;


	for (*s = 0; *s != '\0'; s++)

	length = *s
	_putchar("0" + length / 10);
	_putchar("0" + length % 10);
}
